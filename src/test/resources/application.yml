spring:
  datasource:
    url: jdbc:h2:mem:cims;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
app:
  seed-demo-data: false
  demo-password: ChangeMe123!
